<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkh.sa.admin.module.system.department.dao.DepartmentDao">


    <select id="listAll" resultType="com.hkh.domain.vo.department.DepartmentVO">
        SELECT department.*,
               employee.actual_name   as managerName,
               parent_department.department_name   as parentName
        FROM department
                 left join employee on department.manager_id = employee.employee_id
                 left join department parent_department on department.parent_id = parent_department.department_id
        order by sort desc
    </select>

    <select id="countSubDepartment" resultType="java.lang.Integer">
        SELECT count(*)
        FROM department
        WHERE parent_id = #{departmentId}
    </select>

    <select id="selectDepartmentVO"
            resultType="com.hkh.domain.vo.department.DepartmentVO">
        SELECT department.*,
               employee.actual_name   as managerName,
               parent_department.department_name   as parentName
        FROM department
                 left join employee on department.manager_id = employee.employee_id
                 left join department parent_department on department.parent_id = parent_department.department_id
        where department.department_id = #{departmentId}
    </select>


</mapper>